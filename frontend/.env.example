# URL publique de l'API Node (utilisée par le navigateur)
NEXT_PUBLIC_API_URL=http://localhost:4000
# URL de l'API vue depuis le serveur Next (même valeur, ou http://127.0.0.1:4000 sur le même VPS)
API_URL=http://localhost:4000
NEXT_PUBLIC_SITE_NAME=Mobil-Home Store
